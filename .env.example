# Configuración opcional para el servidor y clientes

# Puerto del servidor gRPC (por defecto: 50051)
GRPC_PORT=50051

# Host del servidor (por defecto: localhost)
GRPC_HOST=localhost

# Nivel de logging (debug, info, warning, error)
LOG_LEVEL=info
